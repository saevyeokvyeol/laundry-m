<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="usersMapper">
	<!-- <resultMap type="user" id="userMap">
		<id column="users_id" property="usersId"/>
		<result column="users_pwd" property="usersPwd"/>
		<result column="users_name" property="usersBame"/>
		<result column="users_phone" property="usersPhone"/>
		<result column="users_type" property="usersType"/>
		<result column="users_address" property="usersAddress"/>
		<result column="users_state" property="usersState"/>
		<result column="users_latitude" property="usersLatitude"/>
		<result column="users_longtitude" property="usersLongtitude"/>
	</resultMap> -->

	<select id="loginUser" parameterType="users"  resultType="users">
		select * from users where user_id = #{userId} and user_pwd = #{userPwd} and user_state='N'
	</select>
	
	<insert id="insertUser" parameterType="users" >
		insert into users values (#{userId}, #{userPwd}, #{userName}, #{userPhone}, #{userType}, #{userAddress}, #{userState}, #{userLatitude}, #{userLongtitude})
	</insert>
	
	<select id="selectByUserId" parameterType="users" >
		select * from users where user_id = #{userId} and user_state='N'
	</select>
	
	<update id="updateUser" parameterType="users" >
		update users set user_pwd = #{userPwd}, user_name = #{userName}, user_address = #{userAddress}, user_latitude = #{userLatitude}, user_longtitude = #{userLongtitude} where user_id = #{userId}
	</update>
	
</mapper>